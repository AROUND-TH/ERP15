<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data>
    
        <record id="custom_product_pricelist_form_view" model="ir.ui.view">
            <field name="name">product.pricelist.form</field>
            <field name="model">product.pricelist</field>
            <field name="inherit_id" ref="product.product_pricelist_view"/>
            <field name="arch" type="xml">

                <xpath expr="//group[@name='pricelist_settings']" position="replace">
                    <group name="pricelist_settings">
                        <field name="currency_id" groups="base.group_multi_currency"/>
                        <field name="active" invisible="1"/>
                        <field name="company_id" groups="base.group_multi_company" options="{'no_create': True}"/>
                    </group>
                    <group name="pricelist_options">
                        <field name="compare_pricelist_base_id" options="{'no_create': True}" domain="[('active', '=', True)]"/>
                    </group>
                </xpath>

            </field>
        </record>

    </data>
</odoo>

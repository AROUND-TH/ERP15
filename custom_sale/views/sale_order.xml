<?xml version="1.0" encoding="utf-8" ?>
<odoo>

    <record id="view_order_form_inherit_sale_stock" model="ir.ui.view">
        <field name="name">sale.order.form.inherit_sale_stock</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale_stock.view_order_form_inherit_sale_stock"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='commitment_date']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>
            <xpath expr="//label[@for='commitment_date']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>
            <xpath expr="//div[@class='o_row']/span" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>
            
        </field>
    </record>

    <record id="view_order_form" model="ir.ui.view">
        <field name="name">sale.order.form.inherit.sale</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//group[@name='partner_details']" position="inside">
                <field name="credit_limit"/>
                <field name="credit_balance"/>
                <field name="credit_available"/>
            </xpath>
            <field name="payment_term_id" position="after">
                <field name="transportation"/>
                <field name="port_of_loading"/>
                <field name="port_of_destination"/>
                <label for="commitment_date" string="Delivery Date"/>
                <div class="o_row">
                    <field name="commitment_date"/>
                    <span class="text-muted" attrs="{'invisible': [('effective_date', '!=', False), ('commitment_date', '!=', False)]}">Expected: <field name="expected_date" widget="date"/></span>
                </div>
                <field name="effective_date" attrs="{'invisible': [('effective_date', '=', False)]}"/>
            </field>
            <xpath expr="//page[@name='order_lines']/field[@name='order_line']/tree/field[@name='product_id']" position="attributes">
                <attribute name="options">{'no_create': True}</attribute>
            </xpath>
            <xpath expr="//group[@name='sale_info']" position="inside">
                <field name="bank_id"/>
            </xpath>

            <field name="tax_totals_json" position="replace">
                <field name="amount_untaxed_nodiscount" string="รวมเป็นเงิน"/>
                <label for="bill_discount" string="ส่วนลด"/>
                <div class="d-flex oe_right">
                    <field class="oe_inline"
                        name="bill_discount_percent"
                        attrs="{'readonly': ['|','|',('state', '=', 'done'),('state', '=', 'cancel'),('invoice_count', '>', 0)]}"
                    />
                    <span class="oe_inline ml-2 mr-5"> % </span>
                    <field class="oe_right"
                        name="bill_discount"
                        attrs="{'readonly': ['|','|',('state', '=', 'done'),('state', '=', 'cancel'),('invoice_count', '>', 0)]}"
                    />
                </div>
                <field name="amount_untaxed" string="ราคาหลังหักส่วนลด"/>
                <field name="amount_tax" string="ภาษี"/>
                <field name="amount_total" string="จำนวนเงินรวมทั้งสิ้น"/>
            </field>
        </field>
    </record>

</odoo>

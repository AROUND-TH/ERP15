<?xml version="1.0" encoding="utf-8" ?>

<odoo>
    <record id="purchase_order_form" model="ir.ui.view">
        <field name="name">purchase.order.form</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form" />
        <field name="arch" type="xml">
            <field name="qty_received" position="after">
                <field name="qty_to_receive" attrs="{'column_invisible': [('parent.state', 'not in', ('purchase', 'done'))]}" optional="hide"/>
            </field>
            <field name="qty_invoiced" position="after">
                <field name="qty_to_invoice" attrs="{'column_invisible': [('parent.state', 'not in', ('purchase', 'done'))]}" optional="hide"/>
            </field>
        </field>
    </record>

    <record id="view_purchase_order_search" model="ir.ui.view">
        <field name="name">purchase.order.view.search</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_view_search" />
        <field name="arch" type="xml">
            <filter name="invoiced" position="after">
                <filter name="open_to_receive" string="Waiting Receipts" domain="[('open_to_receive','=',True)]"/>
            </filter>
        </field>
    </record>
</odoo>
